apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-volume
  labels:
    app: db
spec:
  storageClassName: default
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi